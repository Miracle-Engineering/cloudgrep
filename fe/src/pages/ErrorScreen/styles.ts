import styled from '@emotion/styled';
import { ComponentType } from 'react';

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const TextPlaceholder: ComponentType<any> = styled.p`
	margin: 20;
	padding: 0 5px;
	font-size: 14px;
	font-weight: 500;
`;

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const ErrorContainer: ComponentType<any> = styled.div`
	margin: 20;
`;
