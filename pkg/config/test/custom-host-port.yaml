# configuration used in TestRootCommand

web:
  host: helloworld
  port: 8082
  prefix: "/"
  skipOpen: false

datastore:
  type: sqlite
  dataSourceName: file::memory:?cache=shared

providers:
  - cloud: aws
